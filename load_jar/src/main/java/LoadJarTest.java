public class LoadJarTest {
    /**
     * 1. 被加载的module
     * 2. 必须有MF文件
     * @param test
     */
    public static void start(String test){
        System.out.println("进入load jar" + test);
    }
}
